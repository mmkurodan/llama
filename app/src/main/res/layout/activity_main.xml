<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/mainScrollView"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:fitsSystemWindows="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="12dp">

        <!-- ① TOP STATUS CARD: API status / speed / temp / active backend -->
        <LinearLayout
            style="@style/Card"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <TextView
                android:id="@+id/apiServerStatusMain"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="API/WebUI: Stopped"
                android:textColor="@color/text_primary"
                android:textSize="15sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/headerStatsView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingTop="6dp"
                android:text="⚡ -- tok/s    🌡 --°C    🧠 --"
                android:textColor="@color/text_secondary"
                android:textSize="13sp" />

        </LinearLayout>

        <!-- ② PRIMARY ACTIONS CARD: API on/off (primary) / Reset + Settings (secondary) -->
        <LinearLayout
            style="@style/Card"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <Button
                android:id="@+id/apiServerButton"
                style="@style/PrimaryButton"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Start API/WebUI" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginTop="8dp">

                <Button
                    android:id="@+id/initModelButton"
                    style="@style/SecondaryButton"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:layout_marginEnd="4dp"
                    android:text="Reset" />

                <Button
                    android:id="@+id/settingsButton"
                    style="@style/SecondaryButton"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:layout_marginStart="4dp"
                    android:text="Settings" />

            </LinearLayout>

        </LinearLayout>

        <!-- ③ WEB UI URL CARD: tap to open in browser, long-press to copy -->
        <LinearLayout
            style="@style/Card"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <TextView
                android:id="@+id/webUiUrlView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Web UI: (start API/WebUI)"
                android:textColor="@color/link_blue"
                android:textSize="13sp"
                android:textStyle="bold" />

        </LinearLayout>

        <!-- ④ DIRECT RUN CARD (collapsed by default) -->
        <LinearLayout
            style="@style/Card"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <TextView
                android:id="@+id/directSectionHeader"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingTop="2dp"
                android:paddingBottom="2dp"
                android:text="▶ Direct Run (tap to expand)"
                android:textColor="@color/text_primary"
                android:textSize="15sp"
                android:textStyle="bold" />

            <LinearLayout
                android:id="@+id/directSectionContent"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingTop="8dp"
                android:visibility="gone">

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Profile:"
                    android:textColor="@color/text_secondary"
                    android:textSize="13sp" />

                <Spinner
                    android:id="@+id/profileSpinner"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="6dp" />

                <TextView
                    android:id="@+id/promptLabel"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Enter Prompt:"
                    android:textColor="@color/text_secondary"
                    android:textSize="14sp" />

                <EditText
                    android:id="@+id/promptInput"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:gravity="top|start"
                    android:hint="Enter your prompt here"
                    android:inputType="textMultiLine"
                    android:maxLines="4"
                    android:minLines="2" />

                <Button
                    android:id="@+id/sendButton"
                    style="@style/PrimaryButton"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:enabled="false"
                    android:text="Send" />

                <TextView
                    android:id="@+id/outputSectionLabel"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:paddingTop="8dp"
                    android:text="Model Output:"
                    android:textColor="@color/text_primary"
                    android:textSize="14sp"
                    android:textStyle="bold" />

                <!-- Response area: bounded to <=50% screen (set in code), scrolls beyond.
                     Selectable/copyable (long-press to copy). -->
                <com.micklab.llama.MaxHeightScrollView
                    android:id="@+id/outputScroll"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:background="@drawable/bg_input"
                    android:scrollbars="vertical">

                    <TextView
                        android:id="@+id/outputView"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:minHeight="80dp"
                        android:padding="12dp"
                        android:textIsSelectable="true"
                        android:text="Output will appear here"
                        android:textSize="14sp" />

                </com.micklab.llama.MaxHeightScrollView>

                <!-- Generation speed (tok/s) + device temperature (also in the top header) -->
                <TextView
                    android:id="@+id/statsView"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:paddingTop="4dp"
                    android:paddingBottom="4dp"
                    android:gravity="end"
                    android:text="⚡ -- tok/s    🌡 --°C"
                    android:textColor="@color/text_secondary"
                    android:textSize="12sp"
                    android:textStyle="bold" />

            </LinearLayout>

        </LinearLayout>

        <!-- ⑤ PROCESSING STATUS / LOG CARD (expanded by default) -->
        <LinearLayout
            style="@style/Card"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <TextView
                android:id="@+id/processingSectionLabel"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingTop="2dp"
                android:paddingBottom="2dp"
                android:text="▼ Processing Status/Logs"
                android:textColor="@color/text_primary"
                android:textSize="15sp"
                android:textStyle="bold" />

            <LinearLayout
                android:id="@+id/processingSectionContent"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingTop="6dp"
                android:visibility="visible">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:paddingTop="2dp"
                    android:paddingBottom="2dp">

                    <Button
                        android:id="@+id/viewLogButton"
                        style="@style/SecondaryButton"
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:layout_marginEnd="3dp"
                        android:paddingLeft="4dp"
                        android:paddingRight="4dp"
                        android:text="View Log"
                        android:textSize="12sp" />

                    <Button
                        android:id="@+id/updateLogButton"
                        style="@style/SecondaryButton"
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:layout_marginEnd="3dp"
                        android:paddingLeft="4dp"
                        android:paddingRight="4dp"
                        android:text="Update"
                        android:textSize="12sp" />

                    <Button
                        android:id="@+id/clearLogButton"
                        style="@style/SecondaryButton"
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:layout_marginEnd="3dp"
                        android:paddingLeft="4dp"
                        android:paddingRight="4dp"
                        android:text="Clear"
                        android:textSize="12sp" />

                    <Button
                        android:id="@+id/downloadLogButton"
                        style="@style/SecondaryButton"
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:paddingLeft="4dp"
                        android:paddingRight="4dp"
                        android:text="Download"
                        android:textSize="12sp" />

                </LinearLayout>

                <!-- Processing status / log: bounded to <=50% screen (set in code), scrolls beyond.
                     Selectable/copyable (long-press to copy). -->
                <com.micklab.llama.MaxHeightScrollView
                    android:id="@+id/logScroll"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:background="@drawable/bg_input"
                    android:scrollbars="vertical">

                    <TextView
                        android:id="@+id/logView"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:minHeight="80dp"
                        android:padding="12dp"
                        android:textIsSelectable="true"
                        android:text="Starting...\n"
                        android:textSize="12sp" />

                </com.micklab.llama.MaxHeightScrollView>

            </LinearLayout>

        </LinearLayout>

    </LinearLayout>
</ScrollView>
